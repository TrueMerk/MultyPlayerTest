using UnityEngine;
using Photon.Pun;

namespace Multiplayer
{
    public class MyPhotonService : MonoBehaviourPunCallbacks
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
